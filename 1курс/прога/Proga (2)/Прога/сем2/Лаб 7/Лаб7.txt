#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <locale.h>
#include <time.h>
#include <conio.h>
#include "Header.h"

using namespace std;

int main(int argc, char* argv[])
{
	int ch;
	setlocale(LC_ALL, "RUS");
	int i;
	time_t t;
	struct tm *nabor;
	time(&t);
	nabor = localtime(&t);
	do {
		printf_s("\n1-перевод из текстового в бинарном файл\n2-поиск в бинарном файле структуру удовлетворяющую условию\n3-Показать записи в бинарном файле\n4-корректировка данных в бинарном файле\n0-Выход\nВаш выбор:");
		scanf_s("%d", &ch);
		switch (ch) {
			case 1:i=TxtToBin(argc, argv); break;
			case 2:FindCh(argc, argv, nabor->tm_year+1900, i); break;
			case 3:Show(argc, argv, i); break;
			case 4:CorrectingThisG(argc, argv, nabor->tm_year+1900, i); break;
			case 0:return 0;
			default: printf_s("Нет такой команды\nPress any key\n"); _getch();
		}
	} while (ch!=0);
	return 0;
}